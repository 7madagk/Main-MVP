{
  "name": "math2-ai-backend",
  "version": "1.0.0",
  "description": "Secure API proxy for Math2 AI chats — hides Anthropic keys from frontend",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
